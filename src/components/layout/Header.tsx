import{useEffect,useState}from'react';import{Link,NavLink}from'react-router-dom';import{Menu,X}from'lucide-react';const links=[['/','Inicio'],['/casas','Casas'],['/nosotros','Loma Aura'],['/contacto','Contacto']];export function Header(){const[scrolled,setScrolled]=useState(false),[open,setOpen]=useState(false);useEffect(()=>{const fn=()=>setScrolled(scrollY>40);fn();addEventListener('scroll',fn,{passive:true});return()=>removeEventListener('scroll',fn)},[]);useEffect(()=>{document.body.style.overflow=open?'hidden':'';return()=>{document.body.style.overflow=''}},[open]);return <header className={`site-header ${scrolled?'scrolled':''} ${open?'menu-open':''}`}><Link to="/" className="wordmark" aria-label="Loma Aura, inicio"><strong>LOMA AURA</strong><span>PRIVATE STAYS</span></Link><nav className="desktop-nav" aria-label="Navegación principal">{links.map(([to,label])=><NavLink key={to} to={to}>{label}</NavLink>)}<Link className="nav-cta" to="/contacto">Consultar disponibilidad</Link></nav><button className="menu-toggle" onClick={()=>setOpen(!open)} aria-expanded={open} aria-label={open?'Cerrar menú':'Abrir menú'}>{open?<X/>:<Menu/>}</button><div className="mobile-menu">{links.map(([to,label],i)=><NavLink key={to} to={to} onClick={()=>setOpen(false)}><span>0{i+1}</span>{label}</NavLink>)}<Link to="/contacto" onClick={()=>setOpen(false)} className="button button-light">CONSULTAR DISPONIBILIDAD</Link></div></header>}
