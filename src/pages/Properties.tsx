import{useState}from'react';import{properties}from'../data/properties';import{PropertyFeature}from'../components/property/PropertyFeature';import{Reveal}from'../components/common/Reveal';type Filter='all'|'two'|'four';export function Properties(){const[filter,setFilter]=useState<Filter>('all');const list=properties.filter(p=>filter==='all'||(filter==='two'?p.guests<=2:p.guests>=4));return <><section className="page-hero simple"><p className="eyebrow">COLECCIÓN LOMA AURA</p><h1>Casas</h1><p>Espacios para quedarse.</p></section><section className="section listing"><div className="container filters"><button className={filter==='all'?'active':''} onClick={()=>setFilter('all')}>Todas</button><button className={filter==='two'?'active':''} onClick={()=>setFilter('two')}>2 huéspedes</button><button className={filter==='four'?'active':''} onClick={()=>setFilter('four')}>4+ huéspedes</button></div><div className="container-wide">{list.length?list.map((p,i)=><Reveal key={p.id}><PropertyFeature property={p} index={i}/></Reveal>):<p className="empty-state">Pronto sumaremos una casa para este tipo de estancia.</p>}</div></section></>}
